import 'package:flutter_test/flutter_test.dart';

void main() {
  group('AutRepository Test', () {
    test('should call remote data source if connectivity is mobile or wifi',
        () {});
  });
}
